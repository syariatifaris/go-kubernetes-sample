apiVersion: v1
kind: Pod
metadata:
  name: simplego
  labels:
    app: simplego
    version: v.1.0
spec:
  containers:
  - name: simplego
    image: simplego:v.1.0
    ports:
    - containerPort: 9090
